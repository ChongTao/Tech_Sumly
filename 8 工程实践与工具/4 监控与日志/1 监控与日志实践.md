# 1 监控与日志实践

监控应围绕用户影响、SLO 和资源饱和度设计。服务至少采集请求量、成功率、P95/P99 延迟、并发、依赖错误、队列积压和 CPU、内存、磁盘、网络。

日志使用结构化字段记录时间、服务、实例、环境、事件、错误码、请求 ID 和 trace ID。敏感字段脱敏，日志轮转和保留策略要防止磁盘占满。不要把用户输入直接当作日志模板，避免日志注入。

告警需要说明影响、阈值、负责人和处理手册。告警过多会导致疲劳；优先对错误预算消耗、用户请求失败、数据丢失和不可恢复积压告警。

